# Optional: copy to .env next to install-total-vfd.sh (or export before running).
# Blob GitHub links work; the installer converts them to a direct download URL.
DEFAULT_ZIP_URL=https://github.com/Thajr100/total_vfd_installer/blob/main/total_vfd.zip
